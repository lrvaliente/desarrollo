pipeline {
	agent { label 'master' }
		stages{
			stage('test'){
				def mvnHome = tool name: 'Apache Maven 3.3.9', type: 'maven'
				steps {
					sh "${mvnHome}/bin/mvn clean compile test";
				}
			}
		}
}