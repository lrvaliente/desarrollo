pipeline {
	agent { label 'master' }
		stages{
			stage('test'){
				steps {
					sh "mvn -version";
				}
			}
			
			stage ('Build application') {
				steps {
					echo "mvn clean install -Dmaven.test.skip-true"
				}
			}
			
			stage ('Other') {
				steps {
					echo "other..."
				}
			}
		}
}